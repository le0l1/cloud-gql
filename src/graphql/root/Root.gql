scalar Date
scalar Number

type Query
type Mutation

interface Node {
  id: ID!
}

type PageInfo {
  hasNextPage: Boolean!
}

"鉴权指令"
directive @auth(requires: Boolean = true) on OBJECT | FIELD_DEFINITION
